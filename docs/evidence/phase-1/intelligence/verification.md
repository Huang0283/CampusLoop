# M7 Phase 1 核查记录

版本：m7-p1-v1.1；日期：2026-09-23。执行者：本次辅助生成会话。性质：本地文档自检，不是 M8 互审或 M10 独立验收。

## 1. 环境与版本

Windows / PowerShell；Git 2.55.0.windows.4。智能组基准 `15d6dd52f6c960164619e55979f9ad3bc89cbee7`；后端候选 `4d204c599e30bcfb358bf1b511961359a91b92a6`。新增文档未提交，尚无交付 commit。具体范围见 [deliverables.md](deliverables.md)。

## 2. 已执行的输入核查

| 操作 | 实际结果 |
| --- | --- |
| 读取用户 Markdown（UTF-8） | 确认 Phase 1 的 AI1-01—05 范围及其候选内容 |
| 查询远程分支并克隆 phase1/intelligence | 基准 SHA 与用户文档一致 |
| 读取 AI-P1 任务与 M3 字段/流程/原型 | 确认五项交付；发现成色、地点、降级差异 |
| 读取 M8 能力矩阵 | 确认既有规则匹配/语义候选边界 |
| 读取后端分支 ER 与文件树 | 已见金额/状态候选；相应目录未见 M6 实体字典、OpenAPI 和后端实现 |

## 3. 非作者复核步骤

1. 从实际交付提交检出，按 deliverables 找到五份文档。本次还未产生提交；不能用基准 SHA 假装包含新增文件。
2. 逐项对照 SR-01—12 的输入输出，检查异常与权限边界。
3. 使用关键词手算案例：标题命中=8/18，描述命中=2/18；重复词不加分；预算上限等值通过，超 1 分拒绝。
4. 执行 MC-01—10 纸面判定，再算无偏好 44.4444 分、偏好命中 58.3333 分；超预算商品不能进入评分。
5. 按 LC-01—10 评审事件、原子版本检查和恢复设计。Phase 1 只评审可实现性，Phase 3 再故障注入验证。
6. 按评估方案 Q1/Q2/Q3 复算：主 P@5=0.3，R@5=0.5，MRR@5=0.75；无答案查询单列。
7. 对每个 handoff 项记录采用/修改/删除、评审人、日期、实际提交及缺陷位置。

## 4. 自检与限制

文档文件检查、相对文件链接检查、公式验算和补丁检查的实际结果见交付包 `checks.json`，原始执行输出见包根目录 `evidence/self-check.json` 和 `evidence/self-check.log`。该检查只覆盖本地文本结构和合成公式，不覆盖真实检索效果、数据库事务、权限服务、通知链路或性能，也不核验 Markdown 标题锚点。

成功/失败/权限/状态边界已经设计，但业务系统尚未执行。没有真实数据集指标、模型效果、团队签字或阶段收口结果。正式 AI1-01—05 验收状态保持待评审，不能仅因文件生成而勾选远程任务。

## 5. v1.1 可复跑自检与原始记录

在解压后的完整 v1.1 交付包根目录，使用 Python 3 标准库执行；本次仅提交 Markdown，单独检出这些文档并不包含自检脚本：

```text
python scripts/m7_phase1/self_check.py --output-dir work/m7-p1-self-check
```

脚本位置：`scripts/m7_phase1/self_check.py`（完整交付包内，未包含在本次 Markdown 提交中）。输出目录包含 `self-check.json` 和 `self-check.log`；失败时退出码为 1，异常栈或未通过项写入同一日志。不存在截图证据时记录“不适用”，不能编造截图路径。

本次实际命令使用 `--output-dir work/m7-p1-self-check-v1.1`。交付包 `evidence/` 保留本次原始输出（不是模板）：启动/结束时间、OS/Python 版本、完整参数、工作区 HEAD 与修改状态、文件及脚本 SHA-256、每项结果和退出码。基准 HEAD 仅标识上下文，`delivery_commit=null` 明确表示本次交付尚未提交。实际结果为 26 项自检通过、退出码 0；其中场景数量检查仅核对定义存在，不代表执行了 SR/MC/LC 业务场景。

原始日志不包含自身或后续打包说明的哈希；发布包 checks.json 另记录文件哈希。日志若显示失败，不能删改失败结果后宣称通过，应修复并保存新的运行记录。

## 6. 正式验收缺口与登记要求

当前不满足任务文件要求的完整非作者验收证据。阶段验收执行人应从阶段集成提交检出；M8 的任务分支互审不能替代 M10/M1 的阶段验收。登记最少包含：执行人、执行时间、实际检出 commit、分支、运行环境、命令或操作步骤、预期结果、实际结果、判定、失败日志/截图路径。

| 验收条目 | 当前状态 | 实际结果/证据 | 后续责任 |
| --- | --- | --- | --- |
| 可检出的新增交付 commit | 未执行 | 未提交，无交付 SHA；基准 SHA 不替代 | M7 |
| M8 组内互审 | 未执行 | 无实际执行人、日期、评审链接 | M8 |
| 成功路径 SR-01/02、MC-01/02 | 待非作者复核 | 仅有设计预期，无非作者记录 | M8/M10 |
| 输入失败/空结果 SR-06/07 | 待非作者复核 | 仅有设计预期，无非作者记录 | M8/M10 |
| 权限/业务状态 SR-08/12、MC-06/09/10 | 待非作者复核 | 仅有设计预期，无非作者记录 | M5/M6/M10 |
| 生命周期 LC-01—10 设计可执行性 | 待非作者复核 | 纸面步骤已列，未故障注入；运行验证在 Phase 3 | M6/M9/M10 |
| M10 阶段集成独立复核 | 未执行 | 尚无实际阶段集成检出提交和复核记录 | M10 |
| M1 范围、期限与阶段收口 | 未执行 | H-09/H-11/H-12 未关闭 | M1 |

因此本次修订只补足可复跑的自检依据和正式验收登记要求，没有将第 2 条流程缺口标记为解决。
