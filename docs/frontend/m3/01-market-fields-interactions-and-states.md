# M3-01 市场与求购字段、交互和状态（第一阶段）

## 页面字段

| 模块 | 主要字段 | 关键枚举/约束 |
|---|---|---|
| 商品列表 | 标题、价格、成色、学校、状态、分类、图片 | 状态：在售、已预约、已售、隐藏、已下架 |
| 商品发布 | 图片、分类、成色、原价、售价、描述、地点 | 最多 5 张 JPG/PNG；售价必填且非负 |
| 商品详情 | 图片、标题、价格、卖家、成色、地点、描述 | 已售/下架时禁止发起交易 |
| 求购发布 | 标题、最低/最高预算、最低成色、地点、有效期 | 最低预算不得高于最高预算；有效期不得早于当前日期 |
| 求购详情 | 标题、预算、成色、地点、分类、截止时间 | 过期或删除状态不可联系 |
| 匹配结果 | 商品、价格、匹配分、匹配原因、建议区间 | 匹配分表示相关程度，不是成交概率 |
| 价格建议 | 建议区间、推荐标价、影响因素、样本范围 | 仅供参考，卖家保留最终定价权 |

## 搜索、筛选与排序

- 商品市场：关键词、分类、价格、成色、地点；综合、最新、价格升序/降序。
- 求购市场：关键词、分类、预算、最低成色；最新、预算升序/降序。
- 筛选变化后页码回到第 1 页；“重置筛选”恢复默认条件。
- 返回列表时应保留筛选条件；当前静态原型未持久化，下一阶段可放入 URL 查询参数。

## 筛选、草稿与离开页面规则

- 商品市场和求购市场把关键词、分类、价格或预算、成色、地点、排序和页码写入 URL 查询参数；从详情返回列表时恢复原查询参数。
- 修改任一筛选条件后页码归 1；点击“重置筛选”清除查询参数并恢复默认排序。
- 商品和求购表单首次发生有效修改后进入“有未保存内容”状态；保存草稿或成功提交后清除该状态。
- 用户通过站内链接、浏览器后退或刷新离开有未保存内容的表单时，提示“内容尚未保存，确认离开？”；选择取消后保留当前输入。
- 草稿保存成功后显示保存时间；草稿保存失败时保留输入并提供重试，不显示虚假的成功提示。
- 第一阶段只冻结上述交互规则；URL 状态、草稿存储和离开拦截在第二阶段制作可点击 Mock，第三阶段接入真实接口。

## 异常状态

| 场景 | 预期反馈 |
|---|---|
| 图片上传中 | 显示单图进度，提交按钮保持可解释状态 |
| 上传失败 | 标记失败图片并提供重试/移除 |
| 格式或数量超限 | 在选择后立即提示，不上传无效文件 |
| 表单校验失败 | 定位到首个错误，保留所有已填内容 |
| 商品已售出/下架/删除 | 展示状态，禁用联系、收藏或编辑中的无效操作 |
| 无权编辑 | 返回 403 或显示无权限状态，不渲染编辑表单 |
| 列表为空 | 区分无发布内容和筛选无结果，并提供发布/重置入口 |
| 匹配服务不可用 | 降级为关键词结果，明确说明未运行智能匹配 |
| 价格建议不可用 | 允许继续手动定价，明确说明建议暂不可用 |

## 商品和求购管理规则

- 商品编辑复用发布字段，入口位于“我的发布”；仅发布者本人可进入编辑态。
- 在售商品允许下架；已下架商品允许重新上架；已售商品不可再次上架或编辑价格。
- 删除商品前必须二次确认，确认内容包含商品名称和“删除后不可恢复”；取消确认不改变商品状态。
- 求购编辑复用发布字段，入口位于本人求购详情或“我的发布”；已过期、已删除求购只读。
- 修改求购条件后，旧匹配结果标记为需要重新计算；第一阶段不伪造新的匹配结果。
- 无权编辑时不渲染编辑表单；直接访问编辑地址进入 `/403` 或明确的无权限状态。

## 跨组待确认

| 问题 | 对接人 | 当前假设 |
|---|---|---|
| 商品、收藏、求购字段和状态枚举 | M6 | 当前字段仅用于静态原型 |
| 搜索和匹配返回字段 | M7 | 必须返回可展示的匹配原因 |
| 价格建议区间、因素和降级字段 | M8 | 不展示自动定价或成交保证 |
| 图片大小、格式、数量限制 | M6/M9 | 暂按最多 5 张 JPG/PNG |
